package edu.bocmst.scheduling.mrcpspmax.instance;

public class RenewableResource extends AbstractResource implements IRenewableResource {

	public RenewableResource(int supply) {
		super(supply);
	}
}
